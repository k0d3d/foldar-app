import { boolean } from "yup";
import { TItemSummaryState, TPendingCartState } from "../app/TPendingCartState";

export const dashboardState = {
  itemSummary: null as TItemSummaryState,
  quickCartItem: null as TPendingCartState,  
}